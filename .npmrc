inject-workspace-packages=true
# Injected workspace packages are hardlinked snapshots; without sync, fresh CI
# installs have no dist/ and consumers fail to resolve types after dependency builds.
sync-injected-deps-after-scripts[]=build
