{
  "name": "work-assistant-copilot",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.18.2",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "vitest run",
    "format": "prettier --write .",
    "db:generate": "prisma generate --schema apps/desktop-agent/prisma/schema.prisma",
    "db:push": "prisma db push --schema apps/desktop-agent/prisma/schema.prisma",
    "runtime": "pnpm --filter @workcopilot/agent-core dev"
  },
  "devDependencies": {
    "@types/node": "latest",
    "dotenv": "^17.4.2",
    "eslint": "latest",
    "prettier": "latest",
    "prisma": "latest",
    "tsx": "latest",
    "turbo": "latest",
    "typescript": "5.9.3",
    "vitest": "latest"
  },
  "dependencies": {
    "@prisma/client": "^7.9.0"
  }
}